export class Socket {
    // gpio pin number
    pin: number;

    // sequential number of socket on strip
    socket: number;

    // display name
    name: string;

    // power state of socket on/off
    state: boolean;
}